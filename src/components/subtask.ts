
export class subtask {
  protected id: string;

  constructor() {
    //this.id = Newid;
  }

  static getById(id: string) {
    let result = new subtask();

    return result;
  }

  run(): number {
    let result = 0;

    return result;
  }
}
